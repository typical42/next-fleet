/vendor/
/node_modules/
/js/
/build/
# The frontend build lands its stylesheets beside the hand-written one; only app.css is source.
# One per Vite entry (vite.config.js), so the pattern rather than a list.
/css/nextfleet-*.css
/css/*.chunk.css
*.log
.php-cs-fixer.cache
/.phpunit.cache/
/playwright-report/
/test-results/

# Local agent tool permissions, not app source.
/.claude/
