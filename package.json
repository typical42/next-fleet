{
  "name": "nextfleet",
  "version": "0.0.1",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "browserslist": [
    "extends @nextcloud/browserslist-config"
  ],
  "scripts": {
    "build": "vite build --mode production",
    "lint": "npm run lint:js && npm run lint:css && npm run lint:types",
    "lint:css": "stylelint \"css/**/*.css\" \"src/**/*.{css,scss,vue}\"",
    "lint:css:fix": "npm run lint:css -- --fix",
    "lint:fix": "npm run lint:js -- --fix",
    "lint:js": "eslint . --ext .js,.cjs,.ts,.vue",
    "lint:types": "tsc --noEmit",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:e2e:docker": "docker run --rm --init --network host -u $(id -u):$(id -g) -e HOME=/tmp -e CI -e NEXTFLEET_URL_NC31 -e NEXTFLEET_URL_NC34 -v \"$PWD\":/w -w /w mcr.microsoft.com/playwright:v1.63.0-noble npx playwright test",
    "test:watch": "vitest",
    "watch": "vite build --mode development --watch"
  },
  "overrides": {
    "fast-xml-parser": "^5.7.0"
  },
  "dependencies": {
    "pinia": "^4.0.3",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@nextcloud/browserslist-config": "^3.1.2",
    "@nextcloud/eslint-config": "^8.4.2",
    "@nextcloud/stylelint-config": "^3.2.2",
    "@nextcloud/vite-config": "^2.5.4",
    "@playwright/test": "^1.63.0",
    "@types/node": "^20.19.43",
    "browserslist": "^4.24.0",
    "eslint": "^8.57.1",
    "sass": "^1.83.0",
    "stylelint": "^17.15.0",
    "stylelint-config-recommended-scss": "^17.0.1",
    "stylelint-config-recommended-vue": "^1.6.1",
    "typescript": "^5.7.0",
    "vite": "^7.3.6",
    "vitest": "^4.1.11"
  }
}
